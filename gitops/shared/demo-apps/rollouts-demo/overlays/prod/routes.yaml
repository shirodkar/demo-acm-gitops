kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: rollouts-demo
  labels:
    app: rollouts-demo
spec:
  to:
    kind: Service
    name: stable-service
    weight: 100
  alternateBackends:
    - kind: Service
      name: canary-service
      weight: 0
  port:
    targetPort: 8080-tcp